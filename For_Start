#include <stdio.h>

int fak (int x)
{
  int i;
  for (i = 0; i < x; i++)
    x *= i;
  return x;
}

int main (void)
{
  int a, i;
  printf("Zahl: ");
  if (scanf("%d", &a) != 1) return -1;
  int anzahl = fak (a);
  
  for (i = 0; i < anzahl; i++)
  printf("Hello World");
  
  return 0;
}
